services:
  - type: web
    name: biblioteca-parcial
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python main.py
    staticPublishPath: ./backend/static